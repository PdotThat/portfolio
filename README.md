# Portfolio — Phatpicha Thatchakornkunlawat

แตกไฟล์ zip แล้วดับเบิลคลิก `index.html` เปิดในเบราว์เซอร์ได้เลย รูปขึ้นครบทุกใบ

```
portfolio/
├── index.html
├── Css/style.css
├── Js/main.js
└── Images/          ← 46 ไฟล์ เปลี่ยนชื่อให้ตรงกับโค้ดแล้ว
```

---

## ⚠️ สิ่งเดียวที่ต้องทำเอง — เปลี่ยนรูปโปรไฟล์

ตอนนี้ `Images/profile.jpg` เป็นรูปชั่วคราว (วงกลมมีตัวอักษร PT)
เพราะยังไม่ได้ส่งรูปโปรไฟล์มา

**วิธีเปลี่ยน:** เอารูปตัวเองมาตั้งชื่อ `profile.jpg` แล้ววางทับไฟล์เดิมในโฟลเดอร์ `Images/`
(แนะนำรูปสี่เหลี่ยมจัตุรัส เช่น 800×800 px หน้าอยู่กลางรูป เพราะถูกครอบเป็นวงกลมในหน้าแรก)

ไม่ต้องแก้โค้ดเลย รูปจะขึ้นทั้งหน้า Home และ About อัตโนมัติ

---

## จุดที่อาจอยากแก้เพิ่ม

| อยากแก้ | ไปที่ |
|---|---|
| ใส่อันดับทีมในงาน IT RERU | `index.html` — ค้นหา `SENIOR CATEGORY` มีคอมเมนต์บอกวิธีอยู่บรรทัดถัดไป |
| แก้ตัวเลขในแถบสถิติหน้า About | `index.html` — ค้นหา `stat-row` |
| เพิ่ม/ลบรูปในสไลด์ | คัดลอกบรรทัด `<div class="carousel-slide">` เพิ่ม แล้ววางรูปใหม่ใน `Images/` |
| ปรับความสูงสไลด์ | `Css/style.css` — ค้นหา `.carousel-container` |

**หมายเหตุเรื่องสไลด์:** รูปแรกของแต่ละชุดต้องมี `class="carousel-slide active"`
ที่เหลือใส่แค่ `class="carousel-slide"` (ถ้าลืม JS จะเลือกรูปแรกให้เองอัตโนมัติ)

---

## ฟีเจอร์ในหน้าเว็บ

- **สไลด์รูป 5 ชุด** — กดลูกศร ⟨ ⟩ หรือใช้ปุ่มลูกศรซ้าย-ขวาบนคีย์บอร์ด มีตัวเลขบอกรูปที่เท่าไหร่
- **กดรูปเพื่อขยายเต็มจอ** — กดที่ไหนก็ได้หรือ Esc เพื่อปิด
- **รองรับมือถือ** — สไลด์และการ์ดปรับขนาดอัตโนมัติ
- **โหลดเร็ว** — รูปทั้งหมดถูกย่อและบีบอัดแล้ว (จาก ~60 MB เหลือ ~9 MB) และใช้ lazy loading

---

## ขั้นตอนต่อไป: ขึ้น AWS

โฟลเดอร์นี้พร้อมอัปโหลดขึ้น Amazon S3 ได้เลย จุดที่ต้องระวังตอนตั้งค่า:

1. อัปโหลดโดยคงโครงสร้างโฟลเดอร์ไว้ (`Css/`, `Js/`, `Images/` ต้องอยู่ระดับเดียวกับ `index.html`)
2. ตั้ง Index document เป็น `index.html`
3. ปิด public access ที่ bucket แล้วให้ CloudFront เข้าถึงผ่าน **Origin Access Control (OAC)** แทน
4. ตั้ง Viewer protocol policy เป็น *Redirect HTTP to HTTPS*
5. ทุกครั้งที่อัปเดตไฟล์ อย่าลืม **CloudFront Invalidation** (`/*`) ไม่งั้นจะยังเห็นของเก่าจาก cache
